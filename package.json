{
  "name": "lottery-contract",
  "version": "1.0.0",
  "description": "A simple Ethereum lottery smart contract",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network rinkeby"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.0",
    "hardhat": "^2.12.0"
  },
  "devDependencies": {},
  "license": "MIT"
}